import spinner from './spinner';
import exit from './exit';
import { error, log } from './logger'

// module.exports = {
//     spinner,
//     exit,
// }
export default {
    spinner,
    exit,
    error,
    log
};